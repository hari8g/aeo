# Template only — do not commit real secrets.
# kubectl create secret generic avp-secrets -n avp \
#   --from-literal=db-host=... \
#   --from-literal=db-password=... \
#   --from-literal=redis-host=... \
#   --from-literal=anthropic-api-key=...
apiVersion: v1
kind: Secret
metadata:
  name: avp-secrets
  namespace: avp
type: Opaque
stringData:
  db-host: "REPLACE_ME"
  db-password: "REPLACE_ME"
  redis-host: "REPLACE_ME"
  anthropic-api-key: "REPLACE_ME"
